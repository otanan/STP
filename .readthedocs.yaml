# Read the Docs configuration
# https://docs.readthedocs.io/en/stable/config-file/v2.html

version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.10"

# Sphinx config is in docs/source/, not docs/
sphinx:
  configuration: docs/source/conf.py
